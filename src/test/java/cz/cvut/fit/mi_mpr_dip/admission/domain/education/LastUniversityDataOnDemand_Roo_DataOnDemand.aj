// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.education;

import cz.cvut.fit.mi_mpr_dip.admission.domain.education.LastUniversity;
import cz.cvut.fit.mi_mpr_dip.admission.domain.education.LastUniversityDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect LastUniversityDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LastUniversityDataOnDemand: @Component;
    
    private Random LastUniversityDataOnDemand.rnd = new SecureRandom();
    
    private List<LastUniversity> LastUniversityDataOnDemand.data;
    
    public LastUniversity LastUniversityDataOnDemand.getNewTransientLastUniversity(int index) {
        LastUniversity obj = new LastUniversity();
        setName(obj, index);
        return obj;
    }
    
    public void LastUniversityDataOnDemand.setName(LastUniversity obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public LastUniversity LastUniversityDataOnDemand.getSpecificLastUniversity(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        LastUniversity obj = data.get(index);
        Long id = obj.getLastUniversityId();
        return LastUniversity.findLastUniversity(id);
    }
    
    public LastUniversity LastUniversityDataOnDemand.getRandomLastUniversity() {
        init();
        LastUniversity obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getLastUniversityId();
        return LastUniversity.findLastUniversity(id);
    }
    
    public boolean LastUniversityDataOnDemand.modifyLastUniversity(LastUniversity obj) {
        return false;
    }
    
    public void LastUniversityDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = LastUniversity.findLastUniversityEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'LastUniversity' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<LastUniversity>();
        for (int i = 0; i < 10; i++) {
            LastUniversity obj = getNewTransientLastUniversity(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
