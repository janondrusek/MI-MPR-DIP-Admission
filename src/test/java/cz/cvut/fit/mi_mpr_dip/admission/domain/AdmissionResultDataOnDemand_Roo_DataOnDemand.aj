// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionResult;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionResultDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AdmissionResultDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AdmissionResultDataOnDemand: @Component;
    
    private Random AdmissionResultDataOnDemand.rnd = new SecureRandom();
    
    private List<AdmissionResult> AdmissionResultDataOnDemand.data;
    
    public AdmissionResult AdmissionResultDataOnDemand.getNewTransientAdmissionResult(int index) {
        AdmissionResult obj = new AdmissionResult();
        setValue(obj, index);
        return obj;
    }
    
    public void AdmissionResultDataOnDemand.setValue(AdmissionResult obj, int index) {
        Double value = new Integer(index).doubleValue();
        obj.setValue(value);
    }
    
    public AdmissionResult AdmissionResultDataOnDemand.getSpecificAdmissionResult(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AdmissionResult obj = data.get(index);
        Long id = obj.getAdmissionResultId();
        return AdmissionResult.findAdmissionResult(id);
    }
    
    public AdmissionResult AdmissionResultDataOnDemand.getRandomAdmissionResult() {
        init();
        AdmissionResult obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getAdmissionResultId();
        return AdmissionResult.findAdmissionResult(id);
    }
    
    public boolean AdmissionResultDataOnDemand.modifyAdmissionResult(AdmissionResult obj) {
        return false;
    }
    
    public void AdmissionResultDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AdmissionResult.findAdmissionResultEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AdmissionResult' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AdmissionResult>();
        for (int i = 0; i < 10; i++) {
            AdmissionResult obj = getNewTransientAdmissionResult(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
