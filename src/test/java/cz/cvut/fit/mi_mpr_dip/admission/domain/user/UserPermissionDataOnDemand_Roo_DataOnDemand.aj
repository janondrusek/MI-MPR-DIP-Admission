// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.user;

import cz.cvut.fit.mi_mpr_dip.admission.domain.user.UserPermission;
import cz.cvut.fit.mi_mpr_dip.admission.domain.user.UserPermissionDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect UserPermissionDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UserPermissionDataOnDemand: @Component;
    
    private Random UserPermissionDataOnDemand.rnd = new SecureRandom();
    
    private List<UserPermission> UserPermissionDataOnDemand.data;
    
    public UserPermission UserPermissionDataOnDemand.getNewTransientUserPermission(int index) {
        UserPermission obj = new UserPermission();
        setName(obj, index);
        return obj;
    }
    
    public void UserPermissionDataOnDemand.setName(UserPermission obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public UserPermission UserPermissionDataOnDemand.getSpecificUserPermission(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        UserPermission obj = data.get(index);
        Long id = obj.getUserPermissionId();
        return UserPermission.findUserPermission(id);
    }
    
    public UserPermission UserPermissionDataOnDemand.getRandomUserPermission() {
        init();
        UserPermission obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getUserPermissionId();
        return UserPermission.findUserPermission(id);
    }
    
    public boolean UserPermissionDataOnDemand.modifyUserPermission(UserPermission obj) {
        return false;
    }
    
    public void UserPermissionDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = UserPermission.findUserPermissionEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'UserPermission' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<UserPermission>();
        for (int i = 0; i < 10; i++) {
            UserPermission obj = getNewTransientUserPermission(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
