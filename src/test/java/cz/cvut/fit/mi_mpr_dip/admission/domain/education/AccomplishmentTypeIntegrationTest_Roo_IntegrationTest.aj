// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.education;

import cz.cvut.fit.mi_mpr_dip.admission.domain.education.AccomplishmentType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.education.AccomplishmentTypeDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.education.AccomplishmentTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AccomplishmentTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AccomplishmentTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AccomplishmentTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AccomplishmentTypeIntegrationTest: @Transactional;
    
    @Autowired
    private AccomplishmentTypeDataOnDemand AccomplishmentTypeIntegrationTest.dod;
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testCountAccomplishmentTypes() {
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", dod.getRandomAccomplishmentType());
        long count = AccomplishmentType.countAccomplishmentTypes();
        Assert.assertTrue("Counter for 'AccomplishmentType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testFindAccomplishmentType() {
        AccomplishmentType obj = dod.getRandomAccomplishmentType();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", obj);
        Long id = obj.getAccomplishmentTypeId();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to provide an identifier", id);
        obj = AccomplishmentType.findAccomplishmentType(id);
        Assert.assertNotNull("Find method for 'AccomplishmentType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AccomplishmentType' returned the incorrect identifier", id, obj.getAccomplishmentTypeId());
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testFindAllAccomplishmentTypes() {
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", dod.getRandomAccomplishmentType());
        long count = AccomplishmentType.countAccomplishmentTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'AccomplishmentType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AccomplishmentType> result = AccomplishmentType.findAllAccomplishmentTypes();
        Assert.assertNotNull("Find all method for 'AccomplishmentType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AccomplishmentType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testFindAccomplishmentTypeEntries() {
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", dod.getRandomAccomplishmentType());
        long count = AccomplishmentType.countAccomplishmentTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AccomplishmentType> result = AccomplishmentType.findAccomplishmentTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AccomplishmentType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AccomplishmentType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testFlush() {
        AccomplishmentType obj = dod.getRandomAccomplishmentType();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", obj);
        Long id = obj.getAccomplishmentTypeId();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to provide an identifier", id);
        obj = AccomplishmentType.findAccomplishmentType(id);
        Assert.assertNotNull("Find method for 'AccomplishmentType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAccomplishmentType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AccomplishmentType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testMergeUpdate() {
        AccomplishmentType obj = dod.getRandomAccomplishmentType();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", obj);
        Long id = obj.getAccomplishmentTypeId();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to provide an identifier", id);
        obj = AccomplishmentType.findAccomplishmentType(id);
        boolean modified =  dod.modifyAccomplishmentType(obj);
        Integer currentVersion = obj.getVersion();
        AccomplishmentType merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getAccomplishmentTypeId(), id);
        Assert.assertTrue("Version for 'AccomplishmentType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", dod.getRandomAccomplishmentType());
        AccomplishmentType obj = dod.getNewTransientAccomplishmentType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AccomplishmentType' identifier to be null", obj.getAccomplishmentTypeId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AccomplishmentType' identifier to no longer be null", obj.getAccomplishmentTypeId());
    }
    
    @Test
    public void AccomplishmentTypeIntegrationTest.testRemove() {
        AccomplishmentType obj = dod.getRandomAccomplishmentType();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to initialize correctly", obj);
        Long id = obj.getAccomplishmentTypeId();
        Assert.assertNotNull("Data on demand for 'AccomplishmentType' failed to provide an identifier", id);
        obj = AccomplishmentType.findAccomplishmentType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AccomplishmentType' with identifier '" + id + "'", AccomplishmentType.findAccomplishmentType(id));
    }
    
}
