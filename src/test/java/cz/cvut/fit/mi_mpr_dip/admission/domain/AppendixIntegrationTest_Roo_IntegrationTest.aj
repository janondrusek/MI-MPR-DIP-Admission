// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.Appendix;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AppendixIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AppendixIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AppendixIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AppendixIntegrationTest: @Transactional;
    
    @Autowired
    private AppendixDataOnDemand AppendixIntegrationTest.dod;
    
    @Test
    public void AppendixIntegrationTest.testCountAppendixes() {
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", dod.getRandomAppendix());
        long count = Appendix.countAppendixes();
        Assert.assertTrue("Counter for 'Appendix' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AppendixIntegrationTest.testFindAppendix() {
        Appendix obj = dod.getRandomAppendix();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", obj);
        Long id = obj.getAppendixId();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to provide an identifier", id);
        obj = Appendix.findAppendix(id);
        Assert.assertNotNull("Find method for 'Appendix' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Appendix' returned the incorrect identifier", id, obj.getAppendixId());
    }
    
    @Test
    public void AppendixIntegrationTest.testFindAllAppendixes() {
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", dod.getRandomAppendix());
        long count = Appendix.countAppendixes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Appendix', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Appendix> result = Appendix.findAllAppendixes();
        Assert.assertNotNull("Find all method for 'Appendix' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Appendix' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AppendixIntegrationTest.testFindAppendixEntries() {
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", dod.getRandomAppendix());
        long count = Appendix.countAppendixes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Appendix> result = Appendix.findAppendixEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Appendix' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Appendix' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AppendixIntegrationTest.testFlush() {
        Appendix obj = dod.getRandomAppendix();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", obj);
        Long id = obj.getAppendixId();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to provide an identifier", id);
        obj = Appendix.findAppendix(id);
        Assert.assertNotNull("Find method for 'Appendix' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAppendix(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Appendix' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AppendixIntegrationTest.testMergeUpdate() {
        Appendix obj = dod.getRandomAppendix();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", obj);
        Long id = obj.getAppendixId();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to provide an identifier", id);
        obj = Appendix.findAppendix(id);
        boolean modified =  dod.modifyAppendix(obj);
        Integer currentVersion = obj.getVersion();
        Appendix merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getAppendixId(), id);
        Assert.assertTrue("Version for 'Appendix' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AppendixIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", dod.getRandomAppendix());
        Appendix obj = dod.getNewTransientAppendix(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Appendix' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Appendix' identifier to be null", obj.getAppendixId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Appendix' identifier to no longer be null", obj.getAppendixId());
    }
    
    @Test
    public void AppendixIntegrationTest.testRemove() {
        Appendix obj = dod.getRandomAppendix();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to initialize correctly", obj);
        Long id = obj.getAppendixId();
        Assert.assertNotNull("Data on demand for 'Appendix' failed to provide an identifier", id);
        obj = Appendix.findAppendix(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Appendix' with identifier '" + id + "'", Appendix.findAppendix(id));
    }
    
}
