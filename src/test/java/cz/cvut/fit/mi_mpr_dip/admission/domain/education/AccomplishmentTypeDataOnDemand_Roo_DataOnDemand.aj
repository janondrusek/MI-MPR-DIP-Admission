// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.education;

import java.security.SecureRandom;
import java.util.List;
import java.util.Random;

import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;

import org.springframework.stereotype.Component;

import com.google.common.collect.Lists;

privileged aspect AccomplishmentTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AccomplishmentTypeDataOnDemand: @Component;
    
    private Random AccomplishmentTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<AccomplishmentType> AccomplishmentTypeDataOnDemand.data;
    
    public AccomplishmentType AccomplishmentTypeDataOnDemand.getNewTransientAccomplishmentType(int index) {
        AccomplishmentType obj = new AccomplishmentType();
        setName(obj, index);
        return obj;
    }
    
    public void AccomplishmentTypeDataOnDemand.setName(AccomplishmentType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public AccomplishmentType AccomplishmentTypeDataOnDemand.getSpecificAccomplishmentType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > data.size() - 1) {
            index = data.size() - 1;
        }
        AccomplishmentType obj = data.get(index);
        Long id = obj.getAccomplishmentTypeId();
        return AccomplishmentType.findAccomplishmentType(id);
    }
    
    public AccomplishmentType AccomplishmentTypeDataOnDemand.getRandomAccomplishmentType() {
        init();
        AccomplishmentType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getAccomplishmentTypeId();
        return AccomplishmentType.findAccomplishmentType(id);
    }
    
    public boolean AccomplishmentTypeDataOnDemand.modifyAccomplishmentType(AccomplishmentType obj) {
        return false;
    }
    
    public void AccomplishmentTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AccomplishmentType.findAccomplishmentTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AccomplishmentType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = Lists.newArrayList();
        for (int i = 0; i < 10; i++) {
            AccomplishmentType obj = getNewTransientAccomplishmentType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (ConstraintViolation<?> cv : e.getConstraintViolations()) {
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
