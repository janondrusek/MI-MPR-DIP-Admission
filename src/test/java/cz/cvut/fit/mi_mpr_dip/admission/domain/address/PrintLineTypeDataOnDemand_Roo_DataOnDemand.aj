// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.address;

import cz.cvut.fit.mi_mpr_dip.admission.domain.address.PrintLineType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.address.PrintLineTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect PrintLineTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PrintLineTypeDataOnDemand: @Component;
    
    private Random PrintLineTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<PrintLineType> PrintLineTypeDataOnDemand.data;
    
    public PrintLineType PrintLineTypeDataOnDemand.getNewTransientPrintLineType(int index) {
        PrintLineType obj = new PrintLineType();
        setName(obj, index);
        return obj;
    }
    
    public void PrintLineTypeDataOnDemand.setName(PrintLineType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public PrintLineType PrintLineTypeDataOnDemand.getSpecificPrintLineType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        PrintLineType obj = data.get(index);
        Long id = obj.getPrintLineTypeId();
        return PrintLineType.findPrintLineType(id);
    }
    
    public PrintLineType PrintLineTypeDataOnDemand.getRandomPrintLineType() {
        init();
        PrintLineType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getPrintLineTypeId();
        return PrintLineType.findPrintLineType(id);
    }
    
    public boolean PrintLineTypeDataOnDemand.modifyPrintLineType(PrintLineType obj) {
        return false;
    }
    
    public void PrintLineTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = PrintLineType.findPrintLineTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'PrintLineType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<PrintLineType>();
        for (int i = 0; i < 10; i++) {
            PrintLineType obj = getNewTransientPrintLineType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
