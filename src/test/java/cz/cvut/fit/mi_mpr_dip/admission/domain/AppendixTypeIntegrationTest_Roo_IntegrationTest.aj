// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixTypeDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AppendixTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AppendixTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AppendixTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AppendixTypeIntegrationTest: @Transactional;
    
    @Autowired
    private AppendixTypeDataOnDemand AppendixTypeIntegrationTest.dod;
    
    @Test
    public void AppendixTypeIntegrationTest.testCountAppendixTypes() {
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", dod.getRandomAppendixType());
        long count = AppendixType.countAppendixTypes();
        Assert.assertTrue("Counter for 'AppendixType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testFindAppendixType() {
        AppendixType obj = dod.getRandomAppendixType();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", obj);
        Long id = obj.getAttachmentTypeId();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to provide an identifier", id);
        obj = AppendixType.findAppendixType(id);
        Assert.assertNotNull("Find method for 'AppendixType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AppendixType' returned the incorrect identifier", id, obj.getAttachmentTypeId());
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testFindAllAppendixTypes() {
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", dod.getRandomAppendixType());
        long count = AppendixType.countAppendixTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'AppendixType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AppendixType> result = AppendixType.findAllAppendixTypes();
        Assert.assertNotNull("Find all method for 'AppendixType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AppendixType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testFindAppendixTypeEntries() {
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", dod.getRandomAppendixType());
        long count = AppendixType.countAppendixTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AppendixType> result = AppendixType.findAppendixTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AppendixType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AppendixType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testFlush() {
        AppendixType obj = dod.getRandomAppendixType();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", obj);
        Long id = obj.getAttachmentTypeId();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to provide an identifier", id);
        obj = AppendixType.findAppendixType(id);
        Assert.assertNotNull("Find method for 'AppendixType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAppendixType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AppendixType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testMergeUpdate() {
        AppendixType obj = dod.getRandomAppendixType();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", obj);
        Long id = obj.getAttachmentTypeId();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to provide an identifier", id);
        obj = AppendixType.findAppendixType(id);
        boolean modified =  dod.modifyAppendixType(obj);
        Integer currentVersion = obj.getVersion();
        AppendixType merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getAttachmentTypeId(), id);
        Assert.assertTrue("Version for 'AppendixType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", dod.getRandomAppendixType());
        AppendixType obj = dod.getNewTransientAppendixType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AppendixType' identifier to be null", obj.getAttachmentTypeId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AppendixType' identifier to no longer be null", obj.getAttachmentTypeId());
    }
    
    @Test
    public void AppendixTypeIntegrationTest.testRemove() {
        AppendixType obj = dod.getRandomAppendixType();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to initialize correctly", obj);
        Long id = obj.getAttachmentTypeId();
        Assert.assertNotNull("Data on demand for 'AppendixType' failed to provide an identifier", id);
        obj = AppendixType.findAppendixType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AppendixType' with identifier '" + id + "'", AppendixType.findAppendixType(id));
    }
    
}
