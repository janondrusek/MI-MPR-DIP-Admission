// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.Term;
import cz.cvut.fit.mi_mpr_dip.admission.domain.TermDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.TermIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect TermIntegrationTest_Roo_IntegrationTest {
    
    declare @type: TermIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: TermIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: TermIntegrationTest: @Transactional;
    
    @Autowired
    private TermDataOnDemand TermIntegrationTest.dod;
    
    @Test
    public void TermIntegrationTest.testCountTerms() {
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", dod.getRandomTerm());
        long count = Term.countTerms();
        Assert.assertTrue("Counter for 'Term' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void TermIntegrationTest.testFindTerm() {
        Term obj = dod.getRandomTerm();
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", obj);
        Long id = obj.getTermId();
        Assert.assertNotNull("Data on demand for 'Term' failed to provide an identifier", id);
        obj = Term.findTerm(id);
        Assert.assertNotNull("Find method for 'Term' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Term' returned the incorrect identifier", id, obj.getTermId());
    }
    
    @Test
    public void TermIntegrationTest.testFindAllTerms() {
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", dod.getRandomTerm());
        long count = Term.countTerms();
        Assert.assertTrue("Too expensive to perform a find all test for 'Term', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Term> result = Term.findAllTerms();
        Assert.assertNotNull("Find all method for 'Term' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Term' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void TermIntegrationTest.testFindTermEntries() {
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", dod.getRandomTerm());
        long count = Term.countTerms();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Term> result = Term.findTermEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Term' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Term' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void TermIntegrationTest.testFlush() {
        Term obj = dod.getRandomTerm();
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", obj);
        Long id = obj.getTermId();
        Assert.assertNotNull("Data on demand for 'Term' failed to provide an identifier", id);
        obj = Term.findTerm(id);
        Assert.assertNotNull("Find method for 'Term' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyTerm(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Term' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void TermIntegrationTest.testMergeUpdate() {
        Term obj = dod.getRandomTerm();
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", obj);
        Long id = obj.getTermId();
        Assert.assertNotNull("Data on demand for 'Term' failed to provide an identifier", id);
        obj = Term.findTerm(id);
        boolean modified =  dod.modifyTerm(obj);
        Integer currentVersion = obj.getVersion();
        Term merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getTermId(), id);
        Assert.assertTrue("Version for 'Term' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void TermIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", dod.getRandomTerm());
        Term obj = dod.getNewTransientTerm(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Term' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Term' identifier to be null", obj.getTermId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Term' identifier to no longer be null", obj.getTermId());
    }
    
    @Test
    public void TermIntegrationTest.testRemove() {
        Term obj = dod.getRandomTerm();
        Assert.assertNotNull("Data on demand for 'Term' failed to initialize correctly", obj);
        Long id = obj.getTermId();
        Assert.assertNotNull("Data on demand for 'Term' failed to provide an identifier", id);
        obj = Term.findTerm(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Term' with identifier '" + id + "'", Term.findTerm(id));
    }
    
}
