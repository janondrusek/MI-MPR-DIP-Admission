// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.EvaluationType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.EvaluationTypeDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.EvaluationTypeIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect EvaluationTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: EvaluationTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: EvaluationTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: EvaluationTypeIntegrationTest: @Transactional;
    
    @Autowired
    private EvaluationTypeDataOnDemand EvaluationTypeIntegrationTest.dod;
    
    @Test
    public void EvaluationTypeIntegrationTest.testCountEvaluationTypes() {
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", dod.getRandomEvaluationType());
        long count = EvaluationType.countEvaluationTypes();
        Assert.assertTrue("Counter for 'EvaluationType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testFindEvaluationType() {
        EvaluationType obj = dod.getRandomEvaluationType();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", obj);
        Long id = obj.getEvaluationTypeId();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to provide an identifier", id);
        obj = EvaluationType.findEvaluationType(id);
        Assert.assertNotNull("Find method for 'EvaluationType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'EvaluationType' returned the incorrect identifier", id, obj.getEvaluationTypeId());
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testFindAllEvaluationTypes() {
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", dod.getRandomEvaluationType());
        long count = EvaluationType.countEvaluationTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'EvaluationType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<EvaluationType> result = EvaluationType.findAllEvaluationTypes();
        Assert.assertNotNull("Find all method for 'EvaluationType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'EvaluationType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testFindEvaluationTypeEntries() {
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", dod.getRandomEvaluationType());
        long count = EvaluationType.countEvaluationTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<EvaluationType> result = EvaluationType.findEvaluationTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'EvaluationType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'EvaluationType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testFlush() {
        EvaluationType obj = dod.getRandomEvaluationType();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", obj);
        Long id = obj.getEvaluationTypeId();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to provide an identifier", id);
        obj = EvaluationType.findEvaluationType(id);
        Assert.assertNotNull("Find method for 'EvaluationType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyEvaluationType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'EvaluationType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testMergeUpdate() {
        EvaluationType obj = dod.getRandomEvaluationType();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", obj);
        Long id = obj.getEvaluationTypeId();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to provide an identifier", id);
        obj = EvaluationType.findEvaluationType(id);
        boolean modified =  dod.modifyEvaluationType(obj);
        Integer currentVersion = obj.getVersion();
        EvaluationType merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getEvaluationTypeId(), id);
        Assert.assertTrue("Version for 'EvaluationType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", dod.getRandomEvaluationType());
        EvaluationType obj = dod.getNewTransientEvaluationType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'EvaluationType' identifier to be null", obj.getEvaluationTypeId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'EvaluationType' identifier to no longer be null", obj.getEvaluationTypeId());
    }
    
    @Test
    public void EvaluationTypeIntegrationTest.testRemove() {
        EvaluationType obj = dod.getRandomEvaluationType();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to initialize correctly", obj);
        Long id = obj.getEvaluationTypeId();
        Assert.assertNotNull("Data on demand for 'EvaluationType' failed to provide an identifier", id);
        obj = EvaluationType.findEvaluationType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'EvaluationType' with identifier '" + id + "'", EvaluationType.findEvaluationType(id));
    }
    
}
