// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.education;

import cz.cvut.fit.mi_mpr_dip.admission.domain.education.SchoolType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.education.SchoolTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect SchoolTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SchoolTypeDataOnDemand: @Component;
    
    private Random SchoolTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<SchoolType> SchoolTypeDataOnDemand.data;
    
    public SchoolType SchoolTypeDataOnDemand.getNewTransientSchoolType(int index) {
        SchoolType obj = new SchoolType();
        return obj;
    }
    
    public SchoolType SchoolTypeDataOnDemand.getSpecificSchoolType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SchoolType obj = data.get(index);
        Long id = obj.getId();
        return SchoolType.findSchoolType(id);
    }
    
    public SchoolType SchoolTypeDataOnDemand.getRandomSchoolType() {
        init();
        SchoolType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return SchoolType.findSchoolType(id);
    }
    
    public boolean SchoolTypeDataOnDemand.modifySchoolType(SchoolType obj) {
        return false;
    }
    
    public void SchoolTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SchoolType.findSchoolTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SchoolType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SchoolType>();
        for (int i = 0; i < 10; i++) {
            SchoolType obj = getNewTransientSchoolType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
