// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.StudyModeDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.study.StudyMode;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect StudyModeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: StudyModeDataOnDemand: @Component;
    
    private Random StudyModeDataOnDemand.rnd = new SecureRandom();
    
    private List<StudyMode> StudyModeDataOnDemand.data;
    
    public StudyMode StudyModeDataOnDemand.getNewTransientStudyMode(int index) {
        StudyMode obj = new StudyMode();
        return obj;
    }
    
    public StudyMode StudyModeDataOnDemand.getSpecificStudyMode(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        StudyMode obj = data.get(index);
        Long id = obj.getId();
        return StudyMode.findStudyMode(id);
    }
    
    public StudyMode StudyModeDataOnDemand.getRandomStudyMode() {
        init();
        StudyMode obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return StudyMode.findStudyMode(id);
    }
    
    public boolean StudyModeDataOnDemand.modifyStudyMode(StudyMode obj) {
        return false;
    }
    
    public void StudyModeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = StudyMode.findStudyModeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'StudyMode' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<StudyMode>();
        for (int i = 0; i < 10; i++) {
            StudyMode obj = getNewTransientStudyMode(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
