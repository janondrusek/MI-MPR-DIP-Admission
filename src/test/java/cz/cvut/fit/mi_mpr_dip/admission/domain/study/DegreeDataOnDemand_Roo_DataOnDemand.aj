// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.study;

import cz.cvut.fit.mi_mpr_dip.admission.domain.study.Degree;
import cz.cvut.fit.mi_mpr_dip.admission.domain.study.DegreeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect DegreeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DegreeDataOnDemand: @Component;
    
    private Random DegreeDataOnDemand.rnd = new SecureRandom();
    
    private List<Degree> DegreeDataOnDemand.data;
    
    public Degree DegreeDataOnDemand.getNewTransientDegree(int index) {
        Degree obj = new Degree();
        setName(obj, index);
        return obj;
    }
    
    public void DegreeDataOnDemand.setName(Degree obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public Degree DegreeDataOnDemand.getSpecificDegree(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Degree obj = data.get(index);
        Long id = obj.getDegreeId();
        return Degree.findDegree(id);
    }
    
    public Degree DegreeDataOnDemand.getRandomDegree() {
        init();
        Degree obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getDegreeId();
        return Degree.findDegree(id);
    }
    
    public boolean DegreeDataOnDemand.modifyDegree(Degree obj) {
        return false;
    }
    
    public void DegreeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Degree.findDegreeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Degree' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Degree>();
        for (int i = 0; i < 10; i++) {
            Degree obj = getNewTransientDegree(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
