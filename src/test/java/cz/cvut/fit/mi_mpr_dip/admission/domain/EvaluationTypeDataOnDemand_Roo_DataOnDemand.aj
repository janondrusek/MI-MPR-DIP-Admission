// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.EvaluationType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.EvaluationTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect EvaluationTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: EvaluationTypeDataOnDemand: @Component;
    
    private Random EvaluationTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<EvaluationType> EvaluationTypeDataOnDemand.data;
    
    public EvaluationType EvaluationTypeDataOnDemand.getNewTransientEvaluationType(int index) {
        EvaluationType obj = new EvaluationType();
        setName(obj, index);
        return obj;
    }
    
    public void EvaluationTypeDataOnDemand.setName(EvaluationType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public EvaluationType EvaluationTypeDataOnDemand.getSpecificEvaluationType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        EvaluationType obj = data.get(index);
        Long id = obj.getEvaluationTypeId();
        return EvaluationType.findEvaluationType(id);
    }
    
    public EvaluationType EvaluationTypeDataOnDemand.getRandomEvaluationType() {
        init();
        EvaluationType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getEvaluationTypeId();
        return EvaluationType.findEvaluationType(id);
    }
    
    public boolean EvaluationTypeDataOnDemand.modifyEvaluationType(EvaluationType obj) {
        return false;
    }
    
    public void EvaluationTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = EvaluationType.findEvaluationTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'EvaluationType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<EvaluationType>();
        for (int i = 0; i < 10; i++) {
            EvaluationType obj = getNewTransientEvaluationType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
