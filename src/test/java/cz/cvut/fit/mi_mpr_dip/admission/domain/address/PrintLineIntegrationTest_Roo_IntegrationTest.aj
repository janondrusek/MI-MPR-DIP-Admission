// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain.address;

import cz.cvut.fit.mi_mpr_dip.admission.domain.address.PrintLine;
import cz.cvut.fit.mi_mpr_dip.admission.domain.address.PrintLineDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.address.PrintLineIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PrintLineIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PrintLineIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PrintLineIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PrintLineIntegrationTest: @Transactional;
    
    @Autowired
    private PrintLineDataOnDemand PrintLineIntegrationTest.dod;
    
    @Test
    public void PrintLineIntegrationTest.testCountPrintLines() {
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", dod.getRandomPrintLine());
        long count = PrintLine.countPrintLines();
        Assert.assertTrue("Counter for 'PrintLine' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PrintLineIntegrationTest.testFindPrintLine() {
        PrintLine obj = dod.getRandomPrintLine();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", obj);
        Long id = obj.getPrintLineId();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to provide an identifier", id);
        obj = PrintLine.findPrintLine(id);
        Assert.assertNotNull("Find method for 'PrintLine' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PrintLine' returned the incorrect identifier", id, obj.getPrintLineId());
    }
    
    @Test
    public void PrintLineIntegrationTest.testFindAllPrintLines() {
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", dod.getRandomPrintLine());
        long count = PrintLine.countPrintLines();
        Assert.assertTrue("Too expensive to perform a find all test for 'PrintLine', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PrintLine> result = PrintLine.findAllPrintLines();
        Assert.assertNotNull("Find all method for 'PrintLine' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PrintLine' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PrintLineIntegrationTest.testFindPrintLineEntries() {
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", dod.getRandomPrintLine());
        long count = PrintLine.countPrintLines();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PrintLine> result = PrintLine.findPrintLineEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PrintLine' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PrintLine' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PrintLineIntegrationTest.testFlush() {
        PrintLine obj = dod.getRandomPrintLine();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", obj);
        Long id = obj.getPrintLineId();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to provide an identifier", id);
        obj = PrintLine.findPrintLine(id);
        Assert.assertNotNull("Find method for 'PrintLine' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPrintLine(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PrintLine' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PrintLineIntegrationTest.testMergeUpdate() {
        PrintLine obj = dod.getRandomPrintLine();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", obj);
        Long id = obj.getPrintLineId();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to provide an identifier", id);
        obj = PrintLine.findPrintLine(id);
        boolean modified =  dod.modifyPrintLine(obj);
        Integer currentVersion = obj.getVersion();
        PrintLine merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getPrintLineId(), id);
        Assert.assertTrue("Version for 'PrintLine' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PrintLineIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", dod.getRandomPrintLine());
        PrintLine obj = dod.getNewTransientPrintLine(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PrintLine' identifier to be null", obj.getPrintLineId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'PrintLine' identifier to no longer be null", obj.getPrintLineId());
    }
    
    @Test
    public void PrintLineIntegrationTest.testRemove() {
        PrintLine obj = dod.getRandomPrintLine();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to initialize correctly", obj);
        Long id = obj.getPrintLineId();
        Assert.assertNotNull("Data on demand for 'PrintLine' failed to provide an identifier", id);
        obj = PrintLine.findPrintLine(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PrintLine' with identifier '" + id + "'", PrintLine.findPrintLine(id));
    }
    
}
