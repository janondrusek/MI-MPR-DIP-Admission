// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionState;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionStateDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionStateIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AdmissionStateIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AdmissionStateIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AdmissionStateIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AdmissionStateIntegrationTest: @Transactional;
    
    @Autowired
    private AdmissionStateDataOnDemand AdmissionStateIntegrationTest.dod;
    
    @Test
    public void AdmissionStateIntegrationTest.testCountAdmissionStates() {
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", dod.getRandomAdmissionState());
        long count = AdmissionState.countAdmissionStates();
        Assert.assertTrue("Counter for 'AdmissionState' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testFindAdmissionState() {
        AdmissionState obj = dod.getRandomAdmissionState();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", obj);
        Long id = obj.getAdmissionStateId();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to provide an identifier", id);
        obj = AdmissionState.findAdmissionState(id);
        Assert.assertNotNull("Find method for 'AdmissionState' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AdmissionState' returned the incorrect identifier", id, obj.getAdmissionStateId());
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testFindAllAdmissionStates() {
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", dod.getRandomAdmissionState());
        long count = AdmissionState.countAdmissionStates();
        Assert.assertTrue("Too expensive to perform a find all test for 'AdmissionState', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AdmissionState> result = AdmissionState.findAllAdmissionStates();
        Assert.assertNotNull("Find all method for 'AdmissionState' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AdmissionState' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testFindAdmissionStateEntries() {
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", dod.getRandomAdmissionState());
        long count = AdmissionState.countAdmissionStates();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AdmissionState> result = AdmissionState.findAdmissionStateEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AdmissionState' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AdmissionState' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testFlush() {
        AdmissionState obj = dod.getRandomAdmissionState();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", obj);
        Long id = obj.getAdmissionStateId();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to provide an identifier", id);
        obj = AdmissionState.findAdmissionState(id);
        Assert.assertNotNull("Find method for 'AdmissionState' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAdmissionState(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AdmissionState' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testMergeUpdate() {
        AdmissionState obj = dod.getRandomAdmissionState();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", obj);
        Long id = obj.getAdmissionStateId();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to provide an identifier", id);
        obj = AdmissionState.findAdmissionState(id);
        boolean modified =  dod.modifyAdmissionState(obj);
        Integer currentVersion = obj.getVersion();
        AdmissionState merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getAdmissionStateId(), id);
        Assert.assertTrue("Version for 'AdmissionState' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", dod.getRandomAdmissionState());
        AdmissionState obj = dod.getNewTransientAdmissionState(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AdmissionState' identifier to be null", obj.getAdmissionStateId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'AdmissionState' identifier to no longer be null", obj.getAdmissionStateId());
    }
    
    @Test
    public void AdmissionStateIntegrationTest.testRemove() {
        AdmissionState obj = dod.getRandomAdmissionState();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to initialize correctly", obj);
        Long id = obj.getAdmissionStateId();
        Assert.assertNotNull("Data on demand for 'AdmissionState' failed to provide an identifier", id);
        obj = AdmissionState.findAdmissionState(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'AdmissionState' with identifier '" + id + "'", AdmissionState.findAdmissionState(id));
    }
    
}
