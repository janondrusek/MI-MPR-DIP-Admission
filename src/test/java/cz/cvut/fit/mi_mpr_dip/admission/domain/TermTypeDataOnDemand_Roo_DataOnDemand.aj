// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.TermType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.TermTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect TermTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TermTypeDataOnDemand: @Component;
    
    private Random TermTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<TermType> TermTypeDataOnDemand.data;
    
    public TermType TermTypeDataOnDemand.getNewTransientTermType(int index) {
        TermType obj = new TermType();
        setName(obj, index);
        return obj;
    }
    
    public void TermTypeDataOnDemand.setName(TermType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public TermType TermTypeDataOnDemand.getSpecificTermType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        TermType obj = data.get(index);
        Long id = obj.getTermTypeId();
        return TermType.findTermType(id);
    }
    
    public TermType TermTypeDataOnDemand.getRandomTermType() {
        init();
        TermType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getTermTypeId();
        return TermType.findTermType(id);
    }
    
    public boolean TermTypeDataOnDemand.modifyTermType(TermType obj) {
        return false;
    }
    
    public void TermTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = TermType.findTermTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'TermType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<TermType>();
        for (int i = 0; i < 10; i++) {
            TermType obj = getNewTransientTermType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
