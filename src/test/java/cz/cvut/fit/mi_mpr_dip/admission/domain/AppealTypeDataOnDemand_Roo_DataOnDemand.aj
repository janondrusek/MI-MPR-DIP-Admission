// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.AppealType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppealTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppealTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppealTypeDataOnDemand: @Component;
    
    private Random AppealTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<AppealType> AppealTypeDataOnDemand.data;
    
    public AppealType AppealTypeDataOnDemand.getNewTransientAppealType(int index) {
        AppealType obj = new AppealType();
        setName(obj, index);
        return obj;
    }
    
    public void AppealTypeDataOnDemand.setName(AppealType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public AppealType AppealTypeDataOnDemand.getSpecificAppealType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppealType obj = data.get(index);
        Long id = obj.getAppealTypeId();
        return AppealType.findAppealType(id);
    }
    
    public AppealType AppealTypeDataOnDemand.getRandomAppealType() {
        init();
        AppealType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getAppealTypeId();
        return AppealType.findAppealType(id);
    }
    
    public boolean AppealTypeDataOnDemand.modifyAppealType(AppealType obj) {
        return false;
    }
    
    public void AppealTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppealType.findAppealTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppealType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppealType>();
        for (int i = 0; i < 10; i++) {
            AppealType obj = getNewTransientAppealType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
