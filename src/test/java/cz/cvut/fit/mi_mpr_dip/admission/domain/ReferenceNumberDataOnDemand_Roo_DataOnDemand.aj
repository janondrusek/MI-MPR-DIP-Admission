// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumber;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumberDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumberType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumberTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ReferenceNumberDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ReferenceNumberDataOnDemand: @Component;
    
    private Random ReferenceNumberDataOnDemand.rnd = new SecureRandom();
    
    private List<ReferenceNumber> ReferenceNumberDataOnDemand.data;
    
    @Autowired
    private ReferenceNumberTypeDataOnDemand ReferenceNumberDataOnDemand.referenceNumberTypeDataOnDemand;
    
    public ReferenceNumber ReferenceNumberDataOnDemand.getNewTransientReferenceNumber(int index) {
        ReferenceNumber obj = new ReferenceNumber();
        setCode(obj, index);
        setExecuted(obj, index);
        setReferenceNumberType(obj, index);
        return obj;
    }
    
    public void ReferenceNumberDataOnDemand.setCode(ReferenceNumber obj, int index) {
        String code = "code_" + index;
        obj.setCode(code);
    }
    
    public void ReferenceNumberDataOnDemand.setExecuted(ReferenceNumber obj, int index) {
        Date executed = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setExecuted(executed);
    }
    
    public void ReferenceNumberDataOnDemand.setReferenceNumberType(ReferenceNumber obj, int index) {
        ReferenceNumberType referenceNumberType = referenceNumberTypeDataOnDemand.getRandomReferenceNumberType();
        obj.setReferenceNumberType(referenceNumberType);
    }
    
    public ReferenceNumber ReferenceNumberDataOnDemand.getSpecificReferenceNumber(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ReferenceNumber obj = data.get(index);
        Long id = obj.getReferenceNumberId();
        return ReferenceNumber.findReferenceNumber(id);
    }
    
    public ReferenceNumber ReferenceNumberDataOnDemand.getRandomReferenceNumber() {
        init();
        ReferenceNumber obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getReferenceNumberId();
        return ReferenceNumber.findReferenceNumber(id);
    }
    
    public boolean ReferenceNumberDataOnDemand.modifyReferenceNumber(ReferenceNumber obj) {
        return false;
    }
    
    public void ReferenceNumberDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ReferenceNumber.findReferenceNumberEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ReferenceNumber' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ReferenceNumber>();
        for (int i = 0; i < 10; i++) {
            ReferenceNumber obj = getNewTransientReferenceNumber(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
