// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumberType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ReferenceNumberTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ReferenceNumberTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ReferenceNumberTypeDataOnDemand: @Component;
    
    private Random ReferenceNumberTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<ReferenceNumberType> ReferenceNumberTypeDataOnDemand.data;
    
    public ReferenceNumberType ReferenceNumberTypeDataOnDemand.getNewTransientReferenceNumberType(int index) {
        ReferenceNumberType obj = new ReferenceNumberType();
        setName(obj, index);
        return obj;
    }
    
    public void ReferenceNumberTypeDataOnDemand.setName(ReferenceNumberType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public ReferenceNumberType ReferenceNumberTypeDataOnDemand.getSpecificReferenceNumberType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ReferenceNumberType obj = data.get(index);
        Long id = obj.getReferenceNumberTypeId();
        return ReferenceNumberType.findReferenceNumberType(id);
    }
    
    public ReferenceNumberType ReferenceNumberTypeDataOnDemand.getRandomReferenceNumberType() {
        init();
        ReferenceNumberType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getReferenceNumberTypeId();
        return ReferenceNumberType.findReferenceNumberType(id);
    }
    
    public boolean ReferenceNumberTypeDataOnDemand.modifyReferenceNumberType(ReferenceNumberType obj) {
        return false;
    }
    
    public void ReferenceNumberTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ReferenceNumberType.findReferenceNumberTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ReferenceNumberType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ReferenceNumberType>();
        for (int i = 0; i < 10; i++) {
            ReferenceNumberType obj = getNewTransientReferenceNumberType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
