// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixType;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AppendixTypeDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppendixTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppendixTypeDataOnDemand: @Component;
    
    private Random AppendixTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<AppendixType> AppendixTypeDataOnDemand.data;
    
    public AppendixType AppendixTypeDataOnDemand.getNewTransientAppendixType(int index) {
        AppendixType obj = new AppendixType();
        setName(obj, index);
        return obj;
    }
    
    public void AppendixTypeDataOnDemand.setName(AppendixType obj, int index) {
        String name = "name_" + index;
        obj.setName(name);
    }
    
    public AppendixType AppendixTypeDataOnDemand.getSpecificAppendixType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppendixType obj = data.get(index);
        Long id = obj.getAttachmentTypeId();
        return AppendixType.findAppendixType(id);
    }
    
    public AppendixType AppendixTypeDataOnDemand.getRandomAppendixType() {
        init();
        AppendixType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getAttachmentTypeId();
        return AppendixType.findAppendixType(id);
    }
    
    public boolean AppendixTypeDataOnDemand.modifyAppendixType(AppendixType obj) {
        return false;
    }
    
    public void AppendixTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppendixType.findAppendixTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppendixType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppendixType>();
        for (int i = 0; i < 10; i++) {
            AppendixType obj = getNewTransientAppendixType(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
