// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.Admission;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.AdmissionIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AdmissionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AdmissionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AdmissionIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AdmissionIntegrationTest: @Transactional;
    
    @Autowired
    private AdmissionDataOnDemand AdmissionIntegrationTest.dod;
    
    @Test
    public void AdmissionIntegrationTest.testCountAdmissions() {
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", dod.getRandomAdmission());
        long count = Admission.countAdmissions();
        Assert.assertTrue("Counter for 'Admission' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AdmissionIntegrationTest.testFindAdmission() {
        Admission obj = dod.getRandomAdmission();
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", obj);
        Long id = obj.getAdmissionId();
        Assert.assertNotNull("Data on demand for 'Admission' failed to provide an identifier", id);
        obj = Admission.findAdmission(id);
        Assert.assertNotNull("Find method for 'Admission' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Admission' returned the incorrect identifier", id, obj.getAdmissionId());
    }
    
    @Test
    public void AdmissionIntegrationTest.testFindAllAdmissions() {
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", dod.getRandomAdmission());
        long count = Admission.countAdmissions();
        Assert.assertTrue("Too expensive to perform a find all test for 'Admission', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Admission> result = Admission.findAllAdmissions();
        Assert.assertNotNull("Find all method for 'Admission' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Admission' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AdmissionIntegrationTest.testFindAdmissionEntries() {
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", dod.getRandomAdmission());
        long count = Admission.countAdmissions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Admission> result = Admission.findAdmissionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Admission' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Admission' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AdmissionIntegrationTest.testFlush() {
        Admission obj = dod.getRandomAdmission();
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", obj);
        Long id = obj.getAdmissionId();
        Assert.assertNotNull("Data on demand for 'Admission' failed to provide an identifier", id);
        obj = Admission.findAdmission(id);
        Assert.assertNotNull("Find method for 'Admission' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAdmission(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Admission' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AdmissionIntegrationTest.testMergeUpdate() {
        Admission obj = dod.getRandomAdmission();
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", obj);
        Long id = obj.getAdmissionId();
        Assert.assertNotNull("Data on demand for 'Admission' failed to provide an identifier", id);
        obj = Admission.findAdmission(id);
        boolean modified =  dod.modifyAdmission(obj);
        Integer currentVersion = obj.getVersion();
        Admission merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getAdmissionId(), id);
        Assert.assertTrue("Version for 'Admission' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AdmissionIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", dod.getRandomAdmission());
        Admission obj = dod.getNewTransientAdmission(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Admission' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Admission' identifier to be null", obj.getAdmissionId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Admission' identifier to no longer be null", obj.getAdmissionId());
    }
    
    @Test
    public void AdmissionIntegrationTest.testRemove() {
        Admission obj = dod.getRandomAdmission();
        Assert.assertNotNull("Data on demand for 'Admission' failed to initialize correctly", obj);
        Long id = obj.getAdmissionId();
        Assert.assertNotNull("Data on demand for 'Admission' failed to provide an identifier", id);
        obj = Admission.findAdmission(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Admission' with identifier '" + id + "'", Admission.findAdmission(id));
    }
    
}
