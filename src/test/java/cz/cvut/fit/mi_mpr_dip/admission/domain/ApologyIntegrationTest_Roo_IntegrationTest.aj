// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package cz.cvut.fit.mi_mpr_dip.admission.domain;

import cz.cvut.fit.mi_mpr_dip.admission.domain.Apology;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ApologyDataOnDemand;
import cz.cvut.fit.mi_mpr_dip.admission.domain.ApologyIntegrationTest;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ApologyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ApologyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ApologyIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ApologyIntegrationTest: @Transactional;
    
    @Autowired
    private ApologyDataOnDemand ApologyIntegrationTest.dod;
    
    @Test
    public void ApologyIntegrationTest.testCountApologys() {
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", dod.getRandomApology());
        long count = Apology.countApologys();
        Assert.assertTrue("Counter for 'Apology' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ApologyIntegrationTest.testFindApology() {
        Apology obj = dod.getRandomApology();
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", obj);
        Long id = obj.getApologyId();
        Assert.assertNotNull("Data on demand for 'Apology' failed to provide an identifier", id);
        obj = Apology.findApology(id);
        Assert.assertNotNull("Find method for 'Apology' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Apology' returned the incorrect identifier", id, obj.getApologyId());
    }
    
    @Test
    public void ApologyIntegrationTest.testFindAllApologys() {
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", dod.getRandomApology());
        long count = Apology.countApologys();
        Assert.assertTrue("Too expensive to perform a find all test for 'Apology', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Apology> result = Apology.findAllApologys();
        Assert.assertNotNull("Find all method for 'Apology' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Apology' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ApologyIntegrationTest.testFindApologyEntries() {
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", dod.getRandomApology());
        long count = Apology.countApologys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Apology> result = Apology.findApologyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Apology' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Apology' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ApologyIntegrationTest.testFlush() {
        Apology obj = dod.getRandomApology();
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", obj);
        Long id = obj.getApologyId();
        Assert.assertNotNull("Data on demand for 'Apology' failed to provide an identifier", id);
        obj = Apology.findApology(id);
        Assert.assertNotNull("Find method for 'Apology' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyApology(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Apology' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ApologyIntegrationTest.testMergeUpdate() {
        Apology obj = dod.getRandomApology();
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", obj);
        Long id = obj.getApologyId();
        Assert.assertNotNull("Data on demand for 'Apology' failed to provide an identifier", id);
        obj = Apology.findApology(id);
        boolean modified =  dod.modifyApology(obj);
        Integer currentVersion = obj.getVersion();
        Apology merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getApologyId(), id);
        Assert.assertTrue("Version for 'Apology' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ApologyIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", dod.getRandomApology());
        Apology obj = dod.getNewTransientApology(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Apology' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Apology' identifier to be null", obj.getApologyId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Apology' identifier to no longer be null", obj.getApologyId());
    }
    
    @Test
    public void ApologyIntegrationTest.testRemove() {
        Apology obj = dod.getRandomApology();
        Assert.assertNotNull("Data on demand for 'Apology' failed to initialize correctly", obj);
        Long id = obj.getApologyId();
        Assert.assertNotNull("Data on demand for 'Apology' failed to provide an identifier", id);
        obj = Apology.findApology(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Apology' with identifier '" + id + "'", Apology.findApology(id));
    }
    
}
